# OpenHouse AI — UI Kit

Developer dashboard + purchaser portal. Light surfaces, warm gold accents, premium property-tech polish.

**Surfaces demonstrated in `index.html`:**
- Login
- Developer dashboard (pipeline KPIs, units table)
- Unit profile
- Purchaser portal (handover timeline)

**Components:** `TopBar`, `Sidebar`, `KpiCard`, `UnitsTable`, `UnitProfile`, `TimelineStep`, `Button`, `Input`, `Badge`.

Source references: `apps/unified-portal/components/Navigation.tsx`, `apps/unified-portal/tailwind.config.ts`.
